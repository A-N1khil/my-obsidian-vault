---
Category:
  - "[[Arrays]]"
URL: https://leetcode.com/problems/intersection-of-two-arrays-ii
Difficulty: "[[Easy]]"
---
## Problem Statement
Given two integer arrays `nums1` and `nums2`, return _an array of their intersection_. Each element in the result must be **unique** , and you may return the result in **any order**.

**Example 1:**

**Input:** `nums1 = [1,2,2,1], nums2 = [2,2]`
**Output:** `[2, 2]`

**Example 2:**

**Input:** `nums1 = [4,9,5], nums2 = [9,4,9,8,4]`
**Output:** `[9,4]`
**Explanation:** `[4,9]` is also accepted.

## Solution

```java
public int[] intersect(int[] nums1, int[] nums2) {  
    int[] frequencyCount = new int[1001]; // Given constraint  
  
    for (int num : nums1) {  
       frequencyCount[num]++;  
    }  
  
    int[] result = new int[Math.max(nums1.length, nums2.length)];  
    int index = 0;  
  
    for (int num : nums2) {  
       if (frequencyCount[num] > 0) {  
          frequencyCount[num]--;  
          result[index++] = num;  
       }  
    }  
  
    return Arrays.copyOf(result, index);  
}
```

## DSA Info


## Explanation

We do the same thing we did in [[349. Intersection of Two Arrays]], but this time, we use frequency counting because we want all the occurrences. 